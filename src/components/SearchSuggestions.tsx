import React from "react";

const SearchSuggestions = () => {
  // const filteredSuggestions = suggestions?.filter((suggestion) =>
  //   suggestion?.includes(query)
  // );

  return (
    <div className="absolute top-28 left-1/2 transform -translate-x-1/2 w-4/6 mt-2 border-2 border-black overflow-scroll">
      <div className="px-4 py-2  w-full h-60">{"ㅁ어ㅗ아"}</div>
    </div>
  );
};

export default SearchSuggestions;
